name: update status.amp.dev
on:
  issues:
    types: [edited]
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: add comment
        run: ${{ github.event.action }}
      - name: incident maintanence
        if: contains(github.event.issue.title, '🌸 Cherry-pick request')
        run: |
          node ./status-page.js ${{ github.event.issue.number }} ${{ github.event.issue.body }}
